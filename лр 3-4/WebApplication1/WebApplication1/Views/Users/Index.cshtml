@using Microsoft.AspNetCore.Identity;
@model List<ApplicationUser>
@inject UserManager<ApplicationUser> UserManager

@{
    ViewData["Title"] = "Управление пользователями";
    Layout = "_Layout";
}

<h2>Управление пользователями</h2>
<h3>@ViewData["Title"]</h3>
@*<div class="btn-block">@Html.ActionLink("Зарегистрировать","Register","Users")</div>*@
<div class="btn-block"><a href="/Identity/Account/Register">Зарегистрировать</a></div>
<table class="table">
    @*<thead>*@
    <tr>
        <td>Действие</td>
        <td>Email</td>
        <td>ФИО</td>
    </tr>
    @*</thead>
        <tbody>*@
    @foreach (var user in Model)
    {
        //var action = await UserManager.IsInRoleAsync(user, "Admin") ? "Забрать права администратора" : "Сделать администратором";
        <tr>
            <td>@Html.ActionLink("Редакт.", "Edit", "Users", new { id = user.Id }) @Html.ActionLink("Уд.", "Remove", "Users", new { id = user.Id }) @*@Html.ActionLink(action,"ToggleAdmin","Users",new { id = user.Id })*@ </td>
            <td>@user.Email</td>
            <td>@user.ToString()</td>
        </tr>
    }
    @*</tbody>*@
</table>

